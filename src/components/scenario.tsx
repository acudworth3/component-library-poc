import React from 'react'

type Props = {
  value: string
  onClose?: string
  data - test - id ?: string
}

export const Scenario = (props: Props) => {
  console.info(props)
  return (
    <div>Scenario </div>
  )
}
